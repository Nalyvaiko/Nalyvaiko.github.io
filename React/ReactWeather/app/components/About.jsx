const React = require('react');

const About = React.createClass({
    render: function () {
        return (
            <h3>About component</h3>
        );
    }
});

module.exports = About;
